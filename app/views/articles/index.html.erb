<ul>
  <% @articles.each do |article| %>
    <li><a href="<%= article_path(article.id) %>"><%= "#{article.title}" %></a> <a data-confirm="Are you sure?" rel="nofollow" data-method="delete" href="<%= "/articles/#{article.id}"%>">🗑</a></li>
  <% end %>
</ul>

<%= link_to "Add New Article", new_article_path %>